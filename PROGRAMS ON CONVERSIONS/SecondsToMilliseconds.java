// Write a program to convert seconds into milliseconds 
// Formula is milliseconds = seconds * 1000
// Second = 1000 milliseconds

class SecondsToMilliseconds {
	public static void main(String args[]) {
	double seconds = 50;
	double milliseconds = seconds * 1000;
	System.out.println("Milliseconds will be "+milliseconds);

// Write a program to convert milliseconds into seconds
// Formula is seconds = milliseconds / 1000

	float milli2 = 600000;
	float seconds2 = milli2 / 1000;
	System.out.println("Seconds will be "+seconds2);
 }
}